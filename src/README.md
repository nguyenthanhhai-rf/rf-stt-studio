# Nguồn của notebook

Notebook `RF_STT_Studio.ipynb` được sinh ra từ đây — đừng sửa JSON của notebook bằng tay.

- `src/cell1.py` — cell cài đặt
- `src/cell2.py` — cell giao diện + xử lý

Sửa xong thì chạy:

```bash
node build.js
```
